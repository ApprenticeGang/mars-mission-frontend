version: 2.1
jobs:
  build:
    docker:
      - image: cimg/node:14.10.0
    steps:
      - checkout
      - run: npm run build
  test:
    docker:
      - image: cimg/node:14.10.0
    steps:
      - run: npm run test:coverage
workflows:
  on_commit:
    jobs:
      - build
      - test
          